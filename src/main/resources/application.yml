spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/users
    username: test
    password: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true